.godot/
*.tmp
export/
dist/

# sprite-gen 작업 산출물 — 중간 프레임·원본 시트까지 남아 수백 MB 규모다.
# 런타임이 쓰는 최종 아트는 assets/sprites/ 에 있으므로 저장소에는 그쪽만 둔다.
# (2026-08-07 이전에 커밋된 일부 기록은 히스토리에 남아 있고, 새로 생기는 것만 제외한다.)
assets/generated/
*.sg-rwlock

# OMC 세션 상태 — 실행할 때마다 바뀌는 로컬 캐시다.
.omc/
